// try to convert tql to an sql ast, then generate as sql


import { test, expect } from '@playwright/test'
//import { compile } from './compile'

test('basic', () => {
    expect(1 + 1).toEqual(2)
})

/*
test('compile', () => {
    compile(['album.ts'], {})
    //expect(1 + 1).toEqual(2)
})


test('parse', () => {
})

*/